export const getStyle = ({colors}) => ({
  cursor: 'pointer',
  border: `1px solid ${colors[6]}`,
  backgroundColor: colors[1],
  color: colors[10],
  borderRadius: '5px / 10px',
  width: '100px',
  textAlign: 'center',
});